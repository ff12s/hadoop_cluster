interface:
  display_name: "Hadoop Cluster"
  short_description: "Repo-specific patterns and workflows for hadoop_cluster"
  default_prompt: "Use the hadoop_cluster repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true